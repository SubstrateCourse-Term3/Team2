# 第七课作业
```text
手动实现 Kitty 和 LinkedItem 的 Encode 和 Decode
fn encode_to<T: Output>(&self, dest: &mut T)
    runtime/src/kitties.rs:25
fn decode<I: Input>(input: &mut I) -> Option<Self>
    runtime/src/linked_item.rs:12
```

